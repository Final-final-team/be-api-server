# 목표

- `$study` 호출 시 질문에 대한 답변을 생성하고 현재 작업 디렉터리의 `study/` 폴더에 Markdown 노트로 누적 저장하는 스킬을 만든다.

# 변경 범위

- `/mnt/c/Users/alswl/.codex/skills/study` 스킬 폴더 생성 및 메타데이터 작성
- 질문/답변을 날짜별 Markdown 파일에 추가하는 보조 스크립트 작성

# 구현 방향

- 스킬 본문에 실행 규칙, 저장 규칙, 파일명 규칙을 명시한다.
- 기본 저장 경로는 `<cwd>/study` 로 두고, 사용자가 별도 경로를 주면 그 경로를 우선한다.
- 답변 저장은 `YYYY-MM-DD.md` 파일에 시각, 질문, 답변 순서로 append 한다.

# 검증 방법

- 스킬 quick validation 실행
- 보조 스크립트를 샘플 질문/답변으로 직접 실행해 Markdown 파일 생성 및 append 형식 확인

# 보류 이슈

- 현재는 질문/답변을 날짜별 파일에 누적한다.
- 파일 분리 기준을 주제별로 바꾸는 요구가 생기면 이후 스킬 개선으로 확장한다.
