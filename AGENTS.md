# 개인용 운영 규칙

이 파일은 현재 로컬 Codex 작업용 운영 규칙이며 저장소에 올리지 않는다.

## 1. 커밋/푸시/PR 작성 규칙

- 모든 커밋 메시지는 한글로 작성한다.
- 모든 푸시 설명과 PR 제목/본문은 한글로 작성한다.
- 커밋과 PR 설명은 실제 반영된 작업 내용을 구체적으로 적는다.
- 한 번에 큰 범위를 올리지 말고, 작업 단위가 보이도록 잘게 나눠 반영한다.

## 2. 설계 및 구현 원칙

- 현재 작업 기준은 백엔드 개발이며, Spring Boot 기반에서 DDD 관점을 우선 고려한다.
- 도메인 경계, 애그리게이트, 상태 전이, 트랜잭션 범위를 먼저 고민하고 코드를 작성한다.
- Spring Boot 사용 시 성능, 확장성, 테스트 용이성, 운영 편의성까지 같이 고려한다.
- 로컬 구현만 보지 말고 프론트 연동, API 계약, CI/CD 영향, 배포 이후 운영까지 함께 고려한다.

## 3. 기술 선택 의사결정 원칙

- 기술, 라이브러리, 패턴, 인프라 도구는 혼자 확정하지 않는다.
- 선택이 필요한 경우 먼저 사용자에게 질문하고, 각 선택지의 장단점을 설명한 뒤 결정한다.
- 독단적으로 새로운 기술을 도입하지 않는다.
- 이미 도입한 기술을 유지할지 교체할지도 같은 기준으로 판단한다.

## 4. 주석 작성 원칙

- 클래스에는 역할과 책임이 드러나도록 주석을 작성한다.
- 메서드에는 왜 필요한지와 핵심 동작이 보이도록 주석을 작성한다.
- 엔티티 컬럼에는 저장 의미가 드러나도록 주석을 작성한다.
- 단순한 코드 읽기를 방해하는 장황한 설명은 피하고, 도메인 의도가 드러나는 주석을 남긴다.

## 5. 로컬 전용 폴더 운영 규칙

- `PLAN/`, `DOCS/`, `WHY/`, `AGENTS.md`는 모두 로컬 전용으로 관리한다.
- 위 경로는 어떤 경우에도 커밋, 푸시, PR에 포함하지 않는다.
- 계획이 필요한 작업은 `PLAN/` 아래에 Markdown으로 정리한 뒤 진행한다.
- 개인 문서와 설계 메모는 `DOCS/` 아래에만 둔다.
- 기술/도구 도입 이유는 `WHY/` 아래에 Markdown으로 정리한다.
- 저장소의 추적 파일이나 공통 문서 대신, 로컬 exclude를 통해 개인 문서를 관리한다.

## 6. PLAN 작성 규칙

- 계획 문서는 `PLAN/` 아래에 Markdown 형식으로 작성한다.
- 파일명은 `<주제>-plan.md` 형식을 우선 사용한다.
- 계획에는 목표, 변경 범위, 구현 방향, 검증 방법, 보류 이슈를 포함한다.

## 7. DOCS 작성 규칙

- 개인 설계 문서, 정책 해석, API 메모, ERD, 테이블 명세는 `DOCS/` 아래에 정리한다.
- 도메인별 문서는 `DOCS/<domain>/` 구조를 우선 사용한다.
- 저장소에 포함될 문서와 혼동되지 않게 개인 참고용이라는 점을 유지한다.

## 8. WHY 작성 규칙

- 기술 선택이나 도구 도입 시 `WHY/` 아래에 이유 문서를 남긴다.
- 문서에는 왜 쓰는지, 대안은 무엇이었는지, 장단점은 무엇인지, 어떤 코드/기술과 연결되는지 적는다.
- 파일명은 `<기술명>.md` 또는 `<기능명>-why.md` 형식을 우선 사용한다.

## 9. 작업 태도

- 구현 전에 현재 코드와 정책을 먼저 확인한다.
- 애매한 선택지는 바로 코딩하지 말고 질문과 비교 설명으로 정리한다.
- 구현 이후에는 테스트, API 계약, 프론트 영향, 배포/운영 영향을 다시 점검한다.
